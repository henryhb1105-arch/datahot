# DataHot 列表性能与内容结构治理

## 2026-08-11 基线

同一份 339 个事件的数据在改造前后测得：

| 资源 | 改造前 | 改造后 | 变化 |
|---|---:|---:|---:|
| `site/data/latest.json` / `latest-lite.json` | 779,661 B | 253,146 B | -67.5% |
| 首页 HTML（同数据完整回滚构建 / 默认构建） | 704,713 B | 80,719 B | -88.5% |
| 首页静态时间轴 | 338 张卡片 | 20 张卡片 | 首屏按需扩展 |

数据每天变化，以上数值是同一数据快照的实测结果；构建日志会持续输出当次 `latest.json → latest-lite.json` 的真实字节数和降幅。若轻量文件不再小于完整文件，构建直接失败。

## 当前路径

- `latest.json` 继续作为详情生成、历史归档和一键回滚的完整数据源。
- `latest-lite.json` 只有标题、摘要、栏目、主题、厂商、时间、热度和信源名称；构建会递归拦截正文、富文本块、原文抓取文本、媒体和原文链接字段。
- 首页只静态输出 20 条，点“加载更多”、搜索或筛选时才下载轻量文件。查询、主题和页码写入 URL；从详情页返回时通过 `sessionStorage` 恢复原 URL 和滚动位置。
- 收藏页只下载轻量文件。详情正文仍只存在于对应的静态详情页。

## 内容结构规则

- 首屏每个厂商最多 4 条，厂商缺失时以主信源作为防刷屏键。
- 首屏单栏目软阈值为 12 条；先让其他栏目在相同质量规则下参与，再允许重要性 ≥70、热度 ≥65、多信源或人工置顶事件越过软阈值。
- 正常信源结构以 20 条为首屏下限；若信源极端单一，宁可暂时少于 20 条，也不放松厂商硬上限。
- 这是展示层排序，不改写事件分类、不删除历史内容，也不以低质量内容凑机械配额。
- `sources.html` 独立展示各栏目在站事件、专项信源和累计采用数；专项信源仍受日期、路径、关键词和每轮候选上限约束。

## 生成 HTML 与回滚

静态 HTML 是可丢弃的发布产物：更新任务从完整数据重建详情页和首页，再发布到 `gh-pages`；跨批次需要累积的只有 `site/data/**` 和 `site/media/**`。现阶段保留静态详情的好处是零运行时服务器、稳定 URL 和低运维成本，代价是每批重建数百个文件。当前规模下构建约数秒，暂不引入前端框架或在线正文 API。

若轻量路径发生线上兼容问题，可在构建环境设置：

```bash
LITE_HOME_ENABLED=false python3 pipeline/build_site.py
```

该次构建会恢复完整静态时间轴、原有前端筛选和收藏页读取 `latest.json` 的路径；数据和详情页无需迁移。恢复默认只需移除变量或设为 `true`。
