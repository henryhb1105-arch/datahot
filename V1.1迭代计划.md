# DataHot V1.1 迭代计划

> 版本：V1.1 · 制定日期：2026-08-09
> 当前线上版本：MVP（https://datahot.xiahongbin.com/）
> 目标：补齐 AI HOT 的三大标志性机制（聚簇、详情页、每日简报），同时修复内容质量漏洞

---

## 一、版本目标

把"能跑"升级为"值得每天刷"：同一事件不再重复出现，每条资讯有站内落地页，每天打开能看到一段"今日数据领域主线"。

## 二、范围与具体方案

### F1. 事件聚簇（优先级最高）

**现状问题**：线上实测《大规模管理AI编码成本》《从分析工程师到上下文工程师》等重复出现 2 次。

**数据结构变更**（latest.json）：

```json
{
  "events": [
    {
      "event_id": "evt_xxx",
      "zh_title": "...", "zh_summary": "...", "reason": "...",
      "category": "...", "heat": 82,
      "items": [ {"id":"...","source":"Databricks Blog","link":"...","published":"..."},
                 {"id":"...","source":"Hacker News","link":"...","published":"..."} ]
    }
  ]
}
```

**聚簇算法**（两段式，控制 LLM 成本）：
1. **粗筛（免费）**：URL 归一化（去 UTM 参数/末尾斜杠）+ 标题 token 相似度（Jaccard > 0.6 进候选对）
2. **精判（LLM）**：候选对逐对问 DeepSeek「这两条是否报道同一事件？是/否」，只对候选对调用，预计每次更新 5-15 对，成本可忽略
3. **合并规则**：同事件多信源合并为一个 event；热度 = 主条目热度 + 每多一个信源 +8 分；`zh_summary` 取信息最全的条目
4. **展示**：时间轴每条底部显示"另有 N 家信源报道：xxx · xxx"

**改动文件**：`run_update.py`（新增 cluster 模块）、`build_site.py`（渲染 event 结构）

---

### F2. 站内详情页

**方案**：
- 每个 event 生成一个静态页：`site/e/<event_id>.html`
- 页面内容：完整中文摘要（比首页更长，5-8 句）+ 推荐理由 + 全部信源链接列表 + 相关事件（同栏目最近 3 条）+ 页尾"查看原文"
- 首页卡片点击行为改为：跳站内详情页，详情页再去原文
- 构建量：每批新增约 10-20 个详情页，旧页不动，静态生成零压力

**额外收益**：每个详情页配 OG meta（标题/摘要），微信/Twitter 分享带卡片；可被搜索引擎收录

**改动文件**：`build_site.py`（新增详情页模板）

---

### F3. 每日数据简报

**方案**：
- 每天第一次定时运行（北京时间 08:17 那批）时，额外调一次 LLM：输入当日 00:00 后的全部事件标题+摘要，输出 100-150 字"今日数据领域主线"
- 生成规则：若当日条目 < 5 条则合并昨日内容生成，避免空简报
- 展示：页面顶部深色简报卡片（UI 设计稿已有），标注"基于今日 N 条资讯 · 生成时间"
- 存储：`latest.json` 增加 `daily_brief` 字段，历史简报存入 `site/data/briefs/YYYY-MM-DD.json`（为将来简报归档页留口）

**改动文件**：`run_update.py`、`build_site.py`

---

### F4. 相关性过滤加固

**现状问题**：媒体源扩容后《Airbnb 测试 AI 搜索》《谷歌地图订餐》等泛 AI 新闻混入。

**方案**：
1. prompt 增加硬规则：「仅当内容直接涉及数据工程/数据分析/BI 工具/数据平台/数据产品实践时为 true；泛 AI 消费应用、AI 硬件、AI 政策八卦一律 false」
2. 加 3 组 few-shot 示例（1 正 2 反），把判断边界钉死
3. 厂商源（自家博客）放宽：默认相关，仅过滤明显的招聘/活动软文

**改动文件**：`run_update.py`（prompt 模板）

---

### F5. HN 条目正文抓取

**现状问题**：HN 源只有标题，LLM 摘要靠脑补，质量明显低于厂商博客条目。

**方案**：HN 条目加工前，先抓原文 HTML 提取前 2000 字符正文（readability 式粗提取：去标签取最长段落块），失败则降级为标题+HN 评论摘要
**成本**：每条多一次 HTTP 抓取，LLM 输入增加约 1500 token，日成本增加 < 0.1 元

**改动文件**：`run_update.py`

---

## 三、明确不做（Out of Scope）

- ❌ 邮件订阅（V1.2）
- ❌ 深色模式、移动端深度优化（V1.2）
- ❌ 解封 Snowflake/Power BI/Tableau（需要自部署 RSSHub，独立任务）
- ❌ 厂商实体识别打标（V1.2，等聚簇稳定后基于 event 做更准确）
- ❌ 用户系统/收藏/评论（静态架构不做，见设计方案）

## 四、交付顺序（三个批次，每批可独立上线）

| 批次 | 内容 | 预估工作量 | 验收标准 |
|---|---|---|---|
| **B1** | F4 过滤加固 + F5 HN 正文抓取 | 0.5 天 | 连续 2 次更新无误收的泛 AI 新闻；HN 条目摘要包含原文具体信息 |
| **B2** | F1 事件聚簇 | 1 天 | 线上不再出现肉眼可见的重复事件；多信源事件显示"另有 N 家报道"；热度叠加生效 |
| **B3** | F2 详情页 + F3 每日简报 | 1.5 天 | 每个事件有可分享的详情页 URL；分享链接带 OG 卡片；每天 08:17 批次后首页出现当日简报 |

**总计约 3 天工作量**，每批交付后立即推送线上验证，不做大爆炸式发布。

## 五、成本估算（增量）

| 项 | 每次更新增量 | 日增量（4 次） |
|---|---|---|
| 聚簇 LLM 判对 | ~10 次调用 | ~40 次 |
| 详情页长摘要 | ~15 条 × 800 token | ~5 万 token |
| 每日简报 | 1 次 × 3000 token | ~3000 token |
| HN 正文抓取 | 无 LLM 成本 | — |
| **合计** | | **约 0.05-0.1 元/天**，可忽略 |

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| 聚簇误判（把不同事件合并） | 宁可漏合不可错合：LLM 判对标准从严；首页提供反馈入口（GitHub Issues 链接） |
| 详情页 URL 变动破坏已分享链接 | event_id 基于主条目链接 hash，生成后不变 |
| 简报生成质量不稳定 | prompt 里给定结构模板（"今日主线是 X：一边…另一边…"），并保留人工改写的入口（直接改 briefs/*.json） |
| CI 运行时间变长 | 当前 ~1 分钟，预计增至 ~2-3 分钟，远低于 Actions 限额 |

## 七、上线检查清单（每批发布后）

- [ ] 线上页面正常渲染，无样式错乱
- [ ] 点击卡片行为符合预期（B3 后：进详情页）
- [ ] Actions 日志无报错，信源健康度正常
- [ ] DeepSeek 调用量/费用无异常尖峰
